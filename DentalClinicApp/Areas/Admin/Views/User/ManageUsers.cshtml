@model IEnumerable<UserListViewModel>
@{
	ViewData["Title"] = "User Profiles";
	int count = 0;
}

<table class="table table-hover">
	<thead>
		<tr>
			<th scope="col">#</th>
			<th scope="col">User</th>
			<th scope="col">Email</th>
			<th scope="col">Role</th>
			<th scope="col">Actions</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var user in Model)
		{
			count++;
			<tr>
				<th scope="row">@count</th>
				<td>@user.Name</td>
				<td>@user.Email</td>
				<td>
					@String.Join(", ", user.RoleNames)
				<td>
					<a asp-area="Admin" asp-action="Edit" asp-controller="User" asp-route-id="@user.Id" class="btn btn-primary">Edit</a>
					<a asp-area="Admin" asp-action="SetRole" asp-controller="User" asp-route-id="@user.Id" class="btn btn-success">Set</a>
						@*<a asp-area="Admin" asp-action="Edit" asp-controller="User" asp-route-id="@user.Id" class="btn btn-block btn-outline-info btn-xs">Edit</a>
				<a asp-area="Admin" asp-action="SetRole" asp-controller="User" asp-route-id="@user.Id" class="btn btn-block btn-outline-warning btn-xs">Set</a>*@
				</td>
			</tr>
		}

	</tbody>
</table>